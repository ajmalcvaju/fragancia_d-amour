rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Anyone can view product/category images
      allow read: if true;
      // Only authenticated admin can upload or delete images
      allow write: if request.auth != null;
    }
  }
}
